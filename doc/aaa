AAA Library

global per/process constructor and destructor

aaa_init()
aaa_fini()

AAA context constructor and destructor

aaa_new();
aaa_free();

aaa_open()

BIND_TYPE_CID: (attr: sess.cid) channel id               
BIND_TYPE_SID: (attr: sess.sid) session id               
BIND_TYPE_KMD: (attr: sess.kmd) keying material derivate 
BIND_TYPE_SEC: (attr: sess.sec) secret           
BIND_TYPE_UID: (attr: user.uid) user indetifier

aaa_bind(bind_type, arg)

aaa_close()
